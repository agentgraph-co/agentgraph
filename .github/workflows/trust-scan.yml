name: Trust Scan
on:
  pull_request:
    branches: [main]

jobs:
  trust-scan:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - uses: ./github-action
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
